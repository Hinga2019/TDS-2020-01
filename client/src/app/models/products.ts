export interface Products{
    id?: number;
    title: string;
    image: string;
    images: string;
    description: string;
    price: number;
    quantity: number;
    short_desc: string;
    cat_id: number;    
}